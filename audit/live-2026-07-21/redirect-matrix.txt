DBS PRODUCTION — HOST / REDIRECT MATRIX
Captured: 2026-07-21 (UTC) via curl, User-Agent "GateAudit"

HOST                     FINAL URL                 HTTP  REDIRECTS  SERVER
https://dbsdc.com/       https://dbsdc.com/        200   0          openresty
https://www.dbsdc.com/   https://www.dbsdc.com/    200   0          openresty

FINDING — CRITICAL (infrastructure, not repo):
  There is NO canonical-host redirect. Both www and non-www return 200
  with identical bodies (byte-identical, verified). The approved target is:

      www.dbsdc.com  ->  301 (permanent)  ->  dbsdc.com

  This redirect is currently MISSING. Both hosts serve the site directly,
  creating duplicate-content exposure. Repository metadata is being set to
  the approved non-www host, but the redirect itself is a MANUAL hosting /
  edge action for Dávid — NOT performed by this work (stop rule: no
  Cloudflare/DNS changes).

NOTE ON CDN:
  Production is served by "openresty" (origin), not through a Cloudflare
  proxy at capture time (no cf-ray / server: cloudflare headers). The single
  /cdn-cgi/ email-decode.min.js <script> present in production HTML is baked
  into the static file (saved from a previously proxied page), not injected
  live. It is removed from authored source in this PR (emails are already
  clean mailto: links).

last-modified of production homepage: Mon, 23 Mar 2026 22:25:28 GMT
  (matches persona branch commit date — consistent with production having
  been deployed from the persona branch).
